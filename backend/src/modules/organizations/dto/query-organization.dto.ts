import { PaginationDto } from '../../../common/dto/pagination.dto';

/** Organizations are searched via the shared `search` term on PaginationDto. */
export class QueryOrganizationDto extends PaginationDto {}
